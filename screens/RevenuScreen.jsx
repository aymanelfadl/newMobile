import { View , Text } from "react-native";

const RevenuScreen = () =>{

    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text style={{color:"black"}}>Revenu!</Text>
        </View>
      );
}
export default RevenuScreen;